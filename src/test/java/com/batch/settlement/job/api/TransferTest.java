package com.batch.settlement.job.api;

import com.batch.settlement.dto.TransferForm;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class TransferTest {

    void transferApi(TransferForm transferForm) {

        //log.info("송금 api 실행 param={}", transferForm);
    }


}
